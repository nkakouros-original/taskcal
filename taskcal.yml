---

outputs:
  - name: Default ICS file output
    type: ics
    dir: '.'
    enabled: true

  - name: WebDAV output
    type: caldav
    host: https://my-endpoint
    username: user
    password: 'app-password'
    enabled: true
